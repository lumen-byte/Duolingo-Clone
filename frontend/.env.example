# For local dev, backend is proxied via next.config.js rewrites to http://localhost:8001
# For production, set the deployed FastAPI URL here (used by next.config.js if provided)
NEXT_PUBLIC_BASE_URL=http://localhost:3000
